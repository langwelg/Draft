services:
  - type: web
    name: esports-organizer
    env: node
    plan: free
    buildCommand: cd esports-organizer && npm install
startCommand: cd esports-organizer && npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-4o-mini
